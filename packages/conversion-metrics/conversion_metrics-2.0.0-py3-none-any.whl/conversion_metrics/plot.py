#AUTOGENERATED! DO NOT EDIT! File to edit: dev/00-demo0.ipynb (unless otherwise specified).

__all__ = ['plot']

#Cell
'''Plot conversion rates and related metrics.
Cite https://www.datacamp.com/courses/analyzing-marketing-campaigns-with-pandas

    Parmaters
    ---------
    dataframe: pandas.DataFrame.
        A wide table and each columns respect to a conversion rate,
        the row index is the Date columns on the x-axis.
    x_rotation_degree: integer.
        default 45.
    x_label_size: integer.
        default 12.'''

import pandas as pd
import matplotlib.pyplot as plt
import numpy as np

def plot(dataframe, x_rotation_degree = 45, x_label_size = 12):
    for column in dataframe:
        # Plot column by dataframe's index
        plt.plot(age_group_df.index.values, dataframe[column])
        # IndexError: tuple index out of range, so add `.values`
        plt.title('Daily ' + str(column) + ' conversion rate\n',
                  size = 16)
        plt.ylabel('Conversion rate', size = 14)
        plt.xlabel('Date', size = x_label_size)
        plt.xticks(rotation = x_rotation_degree)
        # Show plot
        plt.show()
        plt.clf()
        # 简洁地实现了批量画图。