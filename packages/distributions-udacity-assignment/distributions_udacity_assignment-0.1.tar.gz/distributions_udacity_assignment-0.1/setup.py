from setuptools import setup

setup(name='distributions_udacity_assignment',
      version='0.1',
      description='Gaussian distributions',
      packages=['distributions_udacity_assignment'],
      zip_safe=False)
