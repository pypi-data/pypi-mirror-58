from setuptools import setup

setup(name='distributions_tl',
      version='0.1',
      description='Gaussian and Binomial Distributions',
      packages=['distributions_tl'],
      author='Teanlouise',
      zip_safe=False)