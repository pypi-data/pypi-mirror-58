#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['ptrender']

package_data = \
{'': ['*']}

install_requires = \
['plumbum', 'pyratemp']

extras_require = \
{'dev': ['flit', 'ipython'], 'yaml': ['strictyaml']}

entry_points = \
{'console_scripts': ['ptrender = ptrender.ptrender:TemplateRenderer',
                     'vwrite = ptrender.ptrender:VWriter']}

setup(name='ptrender',
      version='0.0.1',
      description='Conveniences for rendering text files from pyratemp templates and json or yaml var files',
      author='Andy Kluger',
      author_email=None,
      url='https://github.com/andydecleyre/ptrender',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      extras_require=extras_require,
      entry_points=entry_points,
     )
