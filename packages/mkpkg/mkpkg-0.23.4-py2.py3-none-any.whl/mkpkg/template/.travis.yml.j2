sudo: false
dist: xenial
language: python

python:
{% for each in travis_supports -%}
- {{ each }}
{% endfor %}
install:
- pip install tox-travis

script:
- tox

after_success:
- tox -e codecov{% if docs %}

addons:
  apt:
    packages:
    - libenchant-dev
{%- endif %}
