from setuptools import setup

setup(name='distrib_pkg',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distrib_pkg'],
      author = 'Nati Aluk',
      zip_safe=False)