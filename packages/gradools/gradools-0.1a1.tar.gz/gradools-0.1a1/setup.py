#!/usr/bin/env python
# setup.py generated by flit for tools that don't yet use PEP 517

from distutils.core import setup

packages = \
['gradools']

package_data = \
{'': ['*'], 'gradools': ['tests/*', 'tests/data/*']}

install_requires = \
['pytoml', 'pandas']

entry_points = \
{'console_scripts': ['gdo-check = gradools.check:main',
                     'gdo-mkfb = gradools.mkfb:main',
                     'gdo-mkstable = gradools.mkstable:main',
                     'gdo-report = gradools.report:main',
                     'gdo-stinit = gradools.stinit:main',
                     'gdo-year = gradools.mconfig:print_year']}

setup(name='gradools',
      version='0.1a1',
      description='gradools',
      author='Matthew Brett',
      author_email='matthew.brett@gmail.com',
      url='https://github.com/matthew-brett/gradools',
      packages=packages,
      package_data=package_data,
      install_requires=install_requires,
      entry_points=entry_points,
      python_requires='>=3.6',
     )
